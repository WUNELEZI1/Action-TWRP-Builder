name: TWRP Builder
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Quick Setup
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android
          sudo apt update -y && sudo apt install -y git ccache python3
          mkdir -p ~/bin
          curl -fsSL https://mirrors.tuna.tsinghua.edu.cn/git/git-repo -o ~/bin/repo
          chmod a+x ~/bin/repo
          export PATH=~/bin:$PATH

      - name: Sync & Build
        run: |
          mkdir build && cd build
          repo init --depth=1 -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp -b twrp-12.1 --no-repo-verify
          repo sync -j4 --no-clone-bundle
          git clone https://github.com/WUNELEZI1/android_device_zyb_tb8786p1_64_k510_wifi -b main device/zyb/tb8786p1_64_k510_wifi
          export ALLOW_MISSING_DEPENDENCIES=true USE_CCACHE=1
          ccache -M 20G
          source build/envsetup.sh
          lunch omni_tb8786p1_64_k510_wifi-eng
          make -j2 vendor_bootimage

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: twrp-img
          path: build/out/target/product/tb8786p1_64_k510_wifi/*.img
